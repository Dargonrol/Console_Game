#pragma once
#include "Settings.h"

//extern void init();
extern inline vector<Settings> initializeGame(int screenResX, int screenResY, vector<string> directories);
extern inline void initializeDirectories(vector<string> dirs);
